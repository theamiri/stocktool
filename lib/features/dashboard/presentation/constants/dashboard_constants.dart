import 'package:flutter/material.dart';
import '../../../../core/theme/app_theme.dart';

class DashboardConstants {
  // Colors
  static const Color primaryGold = AppTheme.primaryGold;
  static const Color backgroundColor = AppTheme.backgroundColor;
  static const Color textColor = AppTheme.textColor;
  static const Color headerTextColor = AppTheme.headerTextColor;
  static const Color cardBackgroundColor = AppTheme.cardBackgroundColor;
  static const Color cardTextColor = AppTheme.cardTextColor;

  // Text
  static const String appTitle = 'STOCK TOOLS';
  static const String userName = 'ROD POP';
  static const String userEmail = 'rodxxx@gmail.com';

  // Dimensions
  static const double headerHeight = AppTheme.headerHeight;
  static const double logoSize = AppTheme.smallLogoSize;
  static const double cardHeight = AppTheme.cardHeight;
  static const double buttonHeight = 60.0;

  // Spacing
  static const double headerPadding = AppTheme.paddingLarge;
  static const double cardSpacing = AppTheme.paddingMedium;
  static const double bottomPadding = AppTheme.paddingHuge;
}
