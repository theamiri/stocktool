class DashboardConstants {
  // Feature-specific text
  static const String appTitle = 'STOCK TOOLS';

  // Fallback user data (used when not authenticated)
  static const String userName = 'Guest User';
  static const String userEmail = 'guest@example.com';

  // Feature-specific dimensions
  static const double buttonHeight = 60.0;
}
